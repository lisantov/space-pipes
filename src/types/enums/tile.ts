export enum ETileStatus {
  FILLED = 'filled',
  STILL = 'stll',
  SOLVED = 'solved',
}

export enum ETileType {
  START = 'start',
  THROUGH = 'through',
  FORK = 'fork',
  ANGLE = 'angle',
}


export enum ETileRotation {
  STILL = 0,
  ONCE = 90,
  TWICE = 180,
  TRIPLE = 270,
}
